# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

*.vscode

easyjson-bootstrap*.go
logs/*
*.iml
vendor/pkg/*
*__debug_bin
.idea/*
*.env
.DS_Store
# Anchored to the repo root: a bare "main" also matches any directory named
# main, which silently excluded java/src/main (Maven's standard layout).
/main
local.env
*.txt
tags
